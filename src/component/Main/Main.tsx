import React from 'react';
import Button from '@material-ui/core/Button';

const Main = (props: any) => {
  return (
    <Button data-testid="button" variant="contained" color="secondary">
      Primar
    </Button>
  );
};

export default Main;
